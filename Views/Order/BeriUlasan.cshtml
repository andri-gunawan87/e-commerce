@model IEnumerable<e_commerce.Datas.Entities.DetailOrder>

@{
    ViewData["Title"] = "DetailOrder";
    Layout = "~/Views/Shared/_NewLayout.cshtml";
}

<h1>DetailOrder</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdProduk)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Harga)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.JumlahBarang)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SubTotal)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Order)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.IdProduk)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Harga)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.JumlahBarang)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.SubTotal)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Order.Id)
                </td>
                @if (ViewBag.IsAdmin)
                {
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                        <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                        <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                    </td>
                }
            </tr>
        }
    </tbody>
</table>

<div class="row">
    <div class="col-md-12">
        <form asp-action="SimpanUlasan" enctype="multipart/form-data">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label for="Rating" class="control-label">Rating</label>
                <input name="Rating" type="number" class="form-control" />
            </div>
            <div class="form-group">
                <label for="FileUlasan" class="control-label">Gambar</label>
                <input name="FileUlasan" type="file" class="form-control" />
            </div>
            <div class="form-group">
                <label for="Komentar" class="control-label">Komentar</label>
                <textarea name="Komentar" class="form-control" /></textarea>
            </div>
            <div class="form-group">
                <input name="IdCustomer" value="@ViewBag.DataOrder.IdCustomer" hidden />
            </div>
            <div class="form-group">
                <input name="IdOrder" class="form-control" value="@ViewBag.DataOrder.Id" hidden></input>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>
